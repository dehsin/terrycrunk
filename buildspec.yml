version: 0.1

phases:
  install:
    runtime-versions:
      node: 12.9.1
  build:
    commands:
      - echo Nothing to do yet

artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/*
    - images/*
